def f(n):
    sum = 0                       #O(1)
    for i in range(1, n + 1):     #O(n)
        sum = sum + i             #O(n)
    return sum                    #O(1)

# Отже складність програми O(n)
# Результат програми - сума арифметичної прогресії
# Тому, можна переписати формулою Sn = n*(a1*an) // 2

def f(n):
    return n * (1 + n) // 2   #O(1)